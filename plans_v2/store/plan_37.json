{
    "execution_plan": [
        {
            "id": 1,
            "description": "Aggregate count of categories by name from the main store database.",
            "database": "postgresql+psycopg://store_main_user:store_main_pass@localhost:5441/store_main",
            "query": "SELECT categories.name AS s1__name, COUNT(categories.id) AS s1__count FROM categories GROUP BY categories.name",
            "depends_on": [],
            "output_columns": [
                {
                    "alias": "s1__name",
                    "source": "categories.name"
                },
                {
                    "alias": "s1__count",
                    "source": "categories.id"
                }
            ],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Aggregate count of product_groups by group_name from the alt store database, restricted to names present in step 1, to enable comparison.",
            "database": "postgresql+psycopg://store_alt_user:store_alt_pass@localhost:5442/store_alt",
            "query": "SELECT product_groups.group_name AS s2__group_name, COUNT(product_groups.gid) AS s2__group_count FROM product_groups WHERE product_groups.group_name IN ($step1.s1__name) GROUP BY product_groups.group_name",
            "depends_on": [
                1
            ],
            "output_columns": [
                {
                    "alias": "s2__group_name",
                    "source": "product_groups.group_name"
                },
                {
                    "alias": "s2__group_count",
                    "source": "product_groups.gid"
                }
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "s2__group_name",
                    "dependency_step_column": "s1__name"
                }
            }
        }
    ],
    "final_output_columns": [
        "s1__name",
        "s1__count",
        "s2__group_count"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": "",
        "distinct": false,
        "group_by": []
    }
}