{
    "execution_plan": [
        {
            "id": 1,
            "description": "Select emails of customers recorded in the EU database who are not marked as being in the US (treating country <> 'US' as 'from EU').",
            "database": "postgresql+psycopg://eu_user:eu_pass@localhost:5437/sales_eu",
            "query": "SELECT email AS s1__email FROM customers WHERE country <> 'US'",
            "depends_on": [],
            "output_columns": [
                {
                    "alias": "s1__email",
                    "source": "customers.email"
                }
            ],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Find customers in the US database whose email matches one of the EU emails (these are migrated based on matching email).",
            "database": "postgresql+psycopg://us_user:us_pass@localhost:5438/sales_us",
            "query": "SELECT email AS s2__email FROM customers WHERE email IN ($step1.s1__email)",
            "depends_on": [
                1
            ],
            "output_columns": [
                {
                    "alias": "s2__email",
                    "source": "customers.email"
                }
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "s2__email",
                    "dependency_step_column": "s1__email"
                }
            }
        }
    ],
    "final_output_columns": [
        "s2__email"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "s2__email",
        "distinct": true,
        "group_by": []
    }
}