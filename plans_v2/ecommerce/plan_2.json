{
    "execution_plan": [
        {
            "id": 1,
            "description": "Compute per-customer number of distinct orders and total ordered items by joining Orders and Order_Items in the orders database; keep only customers with >2 orders and >=3 items.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT Orders.customer_id AS s1__customer_id, COUNT(DISTINCT Orders.order_id) AS s1__order_count, COUNT(Order_Items.order_item_id) AS s1__item_count FROM Orders JOIN Order_Items ON Orders.order_id = Order_Items.order_id GROUP BY Orders.customer_id HAVING COUNT(DISTINCT Orders.order_id) > 2 AND COUNT(Order_Items.order_item_id) >= 3;",
            "depends_on": [],
            "output_columns": [
                {
                    "alias": "s1__customer_id",
                    "source": "Orders.customer_id"
                },
                {
                    "alias": "s1__order_count",
                    "source": "Orders.order_id"
                },
                {
                    "alias": "s1__item_count",
                    "source": "Order_Items.order_item_id"
                }
            ],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve customer first and last names from the users database for the customer_ids found in step 1.",
            "database": "postgresql+psycopg://appuser:root@localhost:5433/users_db",
            "query": "SELECT customers.customer_id AS s2__customer_id, customers.customer_first_name AS s2__first_name, customers.customer_last_name AS s2__last_name FROM customers WHERE customers.customer_id IN ($step1.s1__customer_id);",
            "depends_on": [
                1
            ],
            "output_columns": [
                {
                    "alias": "s2__customer_id",
                    "source": "customers.customer_id"
                },
                {
                    "alias": "s2__first_name",
                    "source": "customers.customer_first_name"
                },
                {
                    "alias": "s2__last_name",
                    "source": "customers.customer_last_name"
                }
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "s2__customer_id",
                    "dependency_step_column": "s1__customer_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "s2__customer_id",
        "s2__first_name",
        "s2__last_name"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": "",
        "distinct": false,
        "group_by": []
    }
}