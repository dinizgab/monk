{
    "execution_plan": [
        {
            "id": 1,
            "description": "Get distinct user IDs who have created posts from the app database.",
            "database": "postgresql+psycopg://app_user:app_pass@localhost:5440/app",
            "query": "SELECT DISTINCT posts.user_id AS s1__user_id FROM posts;",
            "depends_on": [],
            "output_columns": [
                {
                    "alias": "s1__user_id",
                    "source": "posts.user_id"
                }
            ],
            "join_info": null
        },
        {
            "id": 2,
            "description": "From the chat database, find users who sent messages and are in the set of post authors (intersection). Return user id and username.",
            "database": "postgresql+psycopg://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT DISTINCT users.id AS s2__id, users.username AS s2__username FROM users INNER JOIN messages ON users.id = messages.sended_by WHERE messages.sended_by IN ($step1.s1__user_id);",
            "depends_on": [
                1
            ],
            "output_columns": [
                {
                    "alias": "s2__id",
                    "source": "users.id"
                },
                {
                    "alias": "s2__username",
                    "source": "users.username"
                }
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "s2__id",
                    "dependency_step_column": "s1__user_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "s2__id",
        "s2__username"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": "",
        "distinct": false,
        "group_by": []
    }
}