{
    "execution_plan": [
        {
            "id": 1,
            "description": "Count the number of orders for each order_status_code in the Orders table so the application can determine which status is least common.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT order_status_code, COUNT(*) AS cnt FROM Orders GROUP BY order_status_code ORDER BY cnt ASC;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "order_status_code",
        "cnt"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}