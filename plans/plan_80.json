{
    "execution_plan": [
        {
            "id": 1,
            "description": "Select color, description, and size for all products whose price is less than the maximum product price (i.e., not at the maximum price).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_color, product_description, product_size FROM Products WHERE product_price < (SELECT MAX(product_price) FROM Products);",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "product_color",
        "product_description",
        "product_size"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}