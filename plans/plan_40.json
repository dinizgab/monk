{
    "execution_plan": [
        {
            "id": 1,
            "description": "Count distinct customers who have at least one ordered item by joining Orders with Order_Items.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT COUNT(DISTINCT o.customer_id) AS distinct_customers FROM Orders o INNER JOIN Order_Items oi ON o.order_id = oi.order_id;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "distinct_customers"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}