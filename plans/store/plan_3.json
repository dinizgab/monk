{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve names of all products from the main store products table.",
            "database": "postgresql://store_main_user:store_main_pass@localhost:5441/store_main",
            "query": "SELECT name FROM products;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve titles of all items from the alternate store items table (treated as product names).",
            "database": "postgresql://store_alt_user:store_alt_pass@localhost:5442/store_alt",
            "query": "SELECT title AS name FROM items;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "name"
    ],
    "final_aggregation": {
        "type": "NONE"
    }
}