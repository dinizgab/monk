{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all category ids and names from the main store.",
            "database": "postgres://store_main_user:store_main_pass@localhost:5441/store_main",
            "query": "SELECT id, name FROM categories;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve product group ids and names from the alternate store whose names match any category name returned in step 1.",
            "database": "postgres://store_alt_user:store_alt_pass@localhost:5442/store_alt",
            "query": "SELECT gid, group_name FROM product_groups WHERE group_name IN ($step1.name);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "group_name",
                    "dependency_step_column": "name"
                }
            }
        }
    ],
    "final_output_columns": [
        "categories.id",
        "categories.name",
        "product_groups.gid",
        "product_groups.group_name"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}