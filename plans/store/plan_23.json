{
    "execution_plan": [
        {
            "id": 1,
            "description": "Count products grouped by category (include categories with zero products).",
            "database": "postgresql+psycopg://store_main_user:store_main_pass@localhost:5441/store_main",
            "query": "SELECT c.id AS category_id, c.name AS category_name, COUNT(p.id) AS product_count FROM categories c LEFT JOIN products p ON p.category_id = c.id GROUP BY c.id, c.name ORDER BY product_count DESC;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "category_id",
        "category_name",
        "product_count"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}