{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve the list of distinct product_ids that have been sold (present in order items).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT DISTINCT product_id FROM Order_Items;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Get products (id and name) that have not been sold by excluding product_ids returned in step 1.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_id, product_name FROM Products WHERE product_id NOT IN ($step1.product_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "LEFT (anti-join / NOT IN)",
                "on": {
                    "current_step_column": "product_id",
                    "dependency_step_column": "product_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "product_id",
        "product_name"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "product_id"
    }
}