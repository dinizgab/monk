{
    "execution_plan": [
        {
            "id": 1,
            "description": "Get the distinct product_ids that have been sold (referenced in order items).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT DISTINCT product_id FROM Order_Items;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Map sold product_ids to their parent product ids (a sold variant counts as its parent kind).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT DISTINCT COALESCE(parent_product_id, product_id) AS sold_parent_id FROM Products WHERE product_id IN ($step1.product_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "product_id",
                    "dependency_step_column": "product_id"
                }
            }
        },
        {
            "id": 3,
            "description": "Return all top-level product kinds (products with parent_product_id IS NULL) that are not present in the sold parent ids — these are product kinds that have not been sold.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_id, product_name FROM Products WHERE parent_product_id IS NULL AND product_id NOT IN ($step2.sold_parent_id);",
            "depends_on": [
                2
            ],
            "join_info": {
                "type": "LEFT",
                "on": {
                    "current_step_column": "product_id",
                    "dependency_step_column": "sold_parent_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "product_id",
        "product_name"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "product_id"
    }
}