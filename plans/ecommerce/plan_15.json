{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all order-item mappings (order_id -> product_id) from the orders database. This provides the product references for every order so the application can compute order totals after obtaining product prices.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT order_id, product_id FROM Order_Items;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve prices for all products referenced by order items returned in step 1.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_id, product_price FROM Products WHERE product_id IN ($step1.product_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "product_id",
                    "dependency_step_column": "product_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "order_id",
        "order_sum"
    ],
    "final_aggregation": {
        "type": "MIN",
        "column": "order_sum"
    }
}