{
    "execution_plan": [
        {
            "id": 1,
            "description": "Find customer IDs for customers who have at least 2 payment methods.",
            "database": "postgresql+psycopg://appuser:root@localhost:5433/users_db",
            "query": "SELECT customer_id FROM customer_payment_methods GROUP BY customer_id HAVING COUNT(*) >= 2;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Get orders (order_id, customer_id, date_order_placed) placed by the customers returned in step 1.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT order_id, customer_id, date_order_placed FROM Orders WHERE customer_id IN ($step1.customer_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "customer_id",
                    "dependency_step_column": "customer_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "date_order_placed"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}