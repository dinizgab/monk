{
    "execution_plan": [
        {
            "id": 1,
            "description": "Compute the number of orders per customer from the Orders table.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT customer_id, COUNT(*) AS orders_count FROM Orders GROUP BY customer_id;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Fetch customer gender for the customers returned in step 1.",
            "database": "postgresql+psycopg://appuser:root@localhost:5433/users_db",
            "query": "SELECT customer_id, gender_code FROM customers WHERE customer_id IN ($step1.customer_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "customer_id",
                    "dependency_step_column": "customer_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "gender_code",
        "orders_count"
    ],
    "final_aggregation": {
        "type": "SUM",
        "column": "orders_count"
    }
}