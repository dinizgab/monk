{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all customers' IDs and first names (buyers).",
            "database": "postgresql+psycopg://appuser:root@localhost:5433/users_db",
            "query": "SELECT customer_id, customer_first_name FROM customers;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve all products' IDs and names.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_id, product_name FROM Products;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 3,
            "description": "Retrieve mappings between customers and products via orders (customer_id -> product_id). This will be used to join buyers to products in the application.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT o.customer_id, oi.product_id FROM Orders o INNER JOIN Order_Items oi ON o.order_id = oi.order_id WHERE o.customer_id IN ($step1.customer_id) AND oi.product_id IN ($step2.product_id);",
            "depends_on": [
                1,
                2
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "customer_id",
                    "dependency_step_column": "customer_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "customer_first_name",
        "product_name"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}