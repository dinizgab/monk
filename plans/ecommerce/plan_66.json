{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve the distinct product IDs that have been ordered (from order items).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT DISTINCT product_id FROM Order_Items;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Fetch product prices for the ordered products identified in step 1.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_id, product_price FROM Products WHERE product_id IN ($step1.product_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "product_id",
                    "dependency_step_column": "product_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "average_product_price"
    ],
    "final_aggregation": {
        "type": "AVG",
        "column": "product_price"
    }
}