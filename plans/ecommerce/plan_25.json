{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all invoices with their status and invoice date from the orders database.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT invoice_number, invoice_status_code, invoice_date FROM Invoices;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve shipment dates for invoices returned by step 1 from the shipments database.",
            "database": "postgresql+psycopg://appuser:root@localhost:5434/shipments_db",
            "query": "SELECT invoice_number, shipment_date FROM shipments WHERE invoice_number IN ($step1.invoice_number);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "invoice_number",
                    "dependency_step_column": "invoice_number"
                }
            }
        }
    ],
    "final_output_columns": [
        "invoice_number",
        "invoice_status_code",
        "invoice_date",
        "shipment_date"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}