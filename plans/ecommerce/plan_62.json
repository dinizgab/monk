{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve the distinct order_item_id values that have been shipped from the shipments database.",
            "database": "postgresql://appuser:root@localhost:5434/shipments_db",
            "query": "SELECT DISTINCT order_item_id FROM shipment_items;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Find the distinct product_id values for the shipped order items from the orders database.",
            "database": "mysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT DISTINCT product_id FROM Order_Items WHERE order_item_id IN ($step1.order_item_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "order_item_id",
                    "dependency_step_column": "order_item_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "shipped_product_count"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "product_id (DISTINCT)"
    }
}