{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all customers' IDs together with their gender codes from the users database.",
            "database": "postgresql+psycopg://appuser:root@localhost:5433/users_db",
            "query": "SELECT customer_id, gender_code FROM customers;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Count orders per customer from the orders database for the customers returned in step 1.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT customer_id, COUNT(*) AS orders_count FROM Orders WHERE customer_id IN ($step1.customer_id) GROUP BY customer_id;",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "customer_id",
                    "dependency_step_column": "customer_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "gender_code",
        "total_orders"
    ],
    "final_aggregation": {
        "type": "SUM",
        "column": "orders_count"
    }
}