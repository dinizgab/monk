{
    "execution_plan": [
        {
            "id": 1,
            "description": "Fetch all users (id and username) from the application database.",
            "database": "postgresql://app_user:app_pass@localhost:5440/app",
            "query": "SELECT id, username FROM users;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Fetch all posts' ids and their user_id for posts authored by the users returned in step 1.",
            "database": "postgresql://app_user:app_pass@localhost:5440/app",
            "query": "SELECT id, user_id FROM posts WHERE user_id IN ($step1.id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "user_id",
                    "dependency_step_column": "id"
                }
            }
        }
    ],
    "final_output_columns": [
        "id",
        "username",
        "post_count"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "posts.id"
    }
}