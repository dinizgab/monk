{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve posts whose content contains a provided word (case-insensitive).",
            "database": "postgres://app_user:app_pass@localhost:5440/app",
            "query": "SELECT id, content, user_id FROM posts WHERE content ILIKE '%' || $1 || '%';",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "id",
        "content",
        "user_id"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}