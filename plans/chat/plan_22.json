{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve the chat_id for every message (one row per message). The application will count messages per chat_id to determine which chat has the most messages.",
            "database": "postgresql://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT chat_id FROM messages;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Fetch chat details (id, name, description) for the chat IDs returned by step 1.",
            "database": "postgresql://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT id, name, description FROM chats WHERE id IN ($step1.chat_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "id",
                    "dependency_step_column": "chat_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "id",
        "name",
        "description",
        "message_count"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "chat_id"
    }
}