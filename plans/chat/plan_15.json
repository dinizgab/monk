{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve usernames and the chat names for every chat membership from the chat database.",
            "database": "postgresql://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT u.username AS username, c.name AS chat_name FROM chat_members cm JOIN users u ON cm.user_id = u.id JOIN chats c ON cm.chat_id = c.id;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "username",
        "chat_name"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}