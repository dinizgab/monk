{
    "execution_plan": [
        {
            "id": 1,
            "description": "Get distinct user IDs that are members of any chat from the chat database.",
            "database": "postgresql://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT DISTINCT user_id FROM chat_members;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Get users from the main app who are not members of any chat (exclude IDs returned by step 1).",
            "database": "postgresql://app_user:app_pass@localhost:5440/app",
            "query": "SELECT id, username, email FROM users WHERE id NOT IN ($step1.user_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "LEFT",
                "on": {
                    "current_step_column": "id",
                    "dependency_step_column": "user_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "id",
        "username",
        "email"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}