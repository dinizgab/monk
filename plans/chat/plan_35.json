{
    "execution_plan": [
        {
            "id": 1,
            "description": "Get all usernames that exist in the main app database.",
            "database": "postgresql://app_user:app_pass@localhost:5440/app",
            "query": "SELECT username FROM users;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Get users from the chat database whose username is not present in the main app (chat-only users).",
            "database": "postgresql://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT id, username, full_name FROM users WHERE username NOT IN ($step1.username);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "LEFT",
                "on": {
                    "current_step_column": "username",
                    "dependency_step_column": "username"
                }
            }
        }
    ],
    "final_output_columns": [
        "id",
        "username",
        "full_name"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}