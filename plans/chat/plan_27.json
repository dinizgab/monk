{
    "execution_plan": [
        {
            "id": 1,
            "description": "Get distinct user IDs that have sent at least one message from the chat database.",
            "database": "postgresql://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT DISTINCT sended_by FROM messages;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Select users from the application database whose IDs are not in the list of message senders (i.e., users who never sent any messages).",
            "database": "postgresql://app_user:app_pass@localhost:5440/app",
            "query": "SELECT id, username, email FROM users WHERE id NOT IN ($step1.sended_by);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "LEFT",
                "on": {
                    "current_step_column": "id",
                    "dependency_step_column": "sended_by"
                }
            }
        }
    ],
    "final_output_columns": [
        "id",
        "username",
        "email"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}