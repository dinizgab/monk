{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all chat membership rows with chat metadata so the application can count users per chat.",
            "database": "postgres://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT cm.chat_id, c.name AS chat_name, cm.user_id FROM chat_members cm JOIN chats c ON cm.chat_id = c.id;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "chat_id",
        "chat_name",
        "user_id"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "user_id"
    }
}