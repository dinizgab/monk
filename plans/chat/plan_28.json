{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all chats that have no messages.",
            "database": "postgresql://app_chat_user:app_chat_pass@localhost:5439/app_chat",
            "query": "SELECT id, name, description, created_at FROM chats c WHERE NOT EXISTS (SELECT 1 FROM messages m WHERE m.chat_id = c.id);",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "id",
        "name",
        "description",
        "created_at"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}