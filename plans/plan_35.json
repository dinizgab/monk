{
    "execution_plan": [
        {
            "id": 1,
            "description": "Get all order IDs that have been shipped (present in shipments).",
            "database": "postgresql+psycopg://appuser:root@localhost:5434/shipments_db",
            "query": "SELECT DISTINCT order_id FROM shipments;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Get all orders that have NOT been shipped by excluding order_ids returned in step 1.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT order_id FROM Orders WHERE order_id NOT IN ($step1.order_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "LEFT",
                "on": {
                    "current_step_column": "order_id",
                    "dependency_step_column": "order_id"
                }
            }
        },
        {
            "id": 3,
            "description": "Retrieve invoice status (and invoice date) for the orders that are not shipped. Assumes invoice_number corresponds to the order identifier.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT invoice_number, invoice_status_code, invoice_date FROM Invoices WHERE invoice_number IN ($step2.order_id);",
            "depends_on": [
                2
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "invoice_number",
                    "dependency_step_column": "order_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "order_id",
        "invoice_number",
        "invoice_status_code",
        "invoice_date"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}