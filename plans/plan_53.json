{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all customer IDs from users_db that do not have any payment method recorded.",
            "database": "postgresql+psycopg://appuser:root@localhost:5433/users_db",
            "query": "SELECT c.customer_id FROM customers c WHERE NOT EXISTS (SELECT 1 FROM customer_payment_methods pm WHERE pm.customer_id = c.customer_id);",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "customer_count"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "customer_id"
    }
}