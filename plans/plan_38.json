{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all orders with their order id and order date.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT order_id, date_order_placed FROM Orders;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve order items (order_id and product_id) for the orders returned in step 1.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT order_id, product_id FROM Order_Items WHERE order_id IN ($step1.order_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "order_id",
                    "dependency_step_column": "order_id"
                }
            }
        },
        {
            "id": 3,
            "description": "Retrieve product prices for the products referenced by the order items returned in step 2.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_id, product_price FROM Products WHERE product_id IN ($step2.product_id);",
            "depends_on": [
                2
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "product_id",
                    "dependency_step_column": "product_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "order_id",
        "date_order_placed",
        "total_amount_paid"
    ],
    "final_aggregation": {
        "type": "SUM",
        "column": "product_price"
    }
}