{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all non-null receipt dates from the receipts table so the application can count receipts per date and determine the top three.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3309/receipts_db",
            "query": "SELECT Date FROM receipts WHERE Date IS NOT NULL;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "Date",
        "receipt_count"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "Date"
    }
}