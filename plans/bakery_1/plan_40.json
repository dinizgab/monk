{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve distinct flavors of items whose food type indicates a cake and whose price is greater than 10.",
            "database": "postgresql://appuser:root@localhost:5436/goods_db",
            "query": "SELECT DISTINCT flavor FROM goods WHERE food ILIKE '%cake%' AND price > 10 AND flavor IS NOT NULL;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "flavor"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}