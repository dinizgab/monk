{
    "execution_plan": [
        {
            "id": 1,
            "description": "Count number of receipts per customer from the receipts database.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3309/receipts_db",
            "query": "SELECT CustomerId AS customer_id, COUNT(*) AS receipt_count FROM receipts GROUP BY CustomerId;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Fetch customer names for the CustomerIds returned in step 1.",
            "database": "postgresql+psycopg://appuser:root@localhost:5435/customers_db",
            "query": "SELECT id, firstname, lastname FROM customers WHERE id IN ($step1.customer_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "id",
                    "dependency_step_column": "customer_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "id",
        "firstname",
        "lastname",
        "receipt_count"
    ],
    "final_aggregation": {
        "type": "MAX",
        "column": "receipt_count"
    }
}