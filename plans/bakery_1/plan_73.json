{
    "execution_plan": [
        {
            "id": 1,
            "description": "Compute the minimum and maximum price for each food type from the goods table, and order results by food type.",
            "database": "postgresql://appuser:root@localhost:5436/goods_db",
            "query": "SELECT food, MIN(price) AS min_price, MAX(price) AS max_price FROM goods GROUP BY food ORDER BY food;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "food",
        "min_price",
        "max_price"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}