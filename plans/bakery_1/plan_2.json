{
    "execution_plan": [
        {
            "id": 1,
            "description": "Select the id and flavor of the single most expensive item whose food type indicates a cake from the goods database.",
            "database": "postgresql+psycopg://appuser:root@localhost:5436/goods_db",
            "query": "SELECT id, flavor FROM goods WHERE food ILIKE '%cake%' AND price IS NOT NULL ORDER BY price DESC LIMIT 1;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "id",
        "flavor"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}