{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all receipts with their associated customer ids (map receipts to customers).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3309/receipts_db",
            "query": "SELECT ReceiptNumber, CustomerId FROM receipts;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve all sold items for the receipts obtained in step 1 (map receipts to goods/items).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3310/items_db",
            "query": "SELECT Receipt, Item FROM items WHERE Receipt IN ($step1.ReceiptNumber);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "Receipt",
                    "dependency_step_column": "ReceiptNumber"
                }
            }
        },
        {
            "id": 3,
            "description": "Retrieve price information for each good/item referenced by the items in step 2.",
            "database": "postgresql+psycopg://appuser:root@localhost:5436/goods_db",
            "query": "SELECT id, price FROM goods WHERE id IN ($step2.Item);",
            "depends_on": [
                2
            ],
            "join_info": {
                "type": "INNER",
                "on": {
                    "current_step_column": "id",
                    "dependency_step_column": "Item"
                }
            }
        }
    ],
    "final_output_columns": [
        "CustomerId"
    ],
    "final_aggregation": {
        "type": "AVG",
        "column": "price"
    }
}