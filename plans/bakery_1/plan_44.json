{
    "execution_plan": [
        {
            "id": 1,
            "description": "Select distinct ids of goods whose price is less than the price of every good whose food = 'Tart'.",
            "database": "postgresql+psycopg://appuser:root@localhost:5436/goods_db",
            "query": "SELECT DISTINCT id FROM goods WHERE price < ALL (SELECT price FROM goods WHERE food = 'Tart');",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "id"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}