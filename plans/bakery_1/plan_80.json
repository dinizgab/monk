{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve distinct visit dates together with the customer IDs who visited on those dates (exclude null customer ids). The application will aggregate these pairs to count customers per date.",
            "database": "mysql+pymysql://appuser:apppass@localhost:3309/receipts_db",
            "query": "SELECT DISTINCT `Date`, CustomerId FROM receipts WHERE CustomerId IS NOT NULL;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "Date",
        "CustomerId"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "CustomerId"
    }
}