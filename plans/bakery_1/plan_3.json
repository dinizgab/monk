{
    "execution_plan": [
        {
            "id": 1,
            "description": "Find the goods entries where food = 'cookie' and return the rows that have the minimum price among cookies (handles ties).",
            "database": "postgresql+psycopg://appuser:root@localhost:5436/goods_db",
            "query": "SELECT id, flavor, price FROM goods WHERE food = 'cookie' AND price = (SELECT MIN(price) FROM goods WHERE food = 'cookie');",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "flavor",
        "price"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}