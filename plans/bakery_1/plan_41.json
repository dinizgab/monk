{
    "execution_plan": [
        {
            "id": 1,
            "description": "Select distinct id and price for goods whose price is below the overall average price of all goods.",
            "database": "postgresql://appuser:root@localhost:5436/goods_db",
            "query": "SELECT DISTINCT id, price FROM goods WHERE price < (SELECT AVG(price) FROM goods);",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "id",
        "price"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}