{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve all shipped item identifiers from the shipment_items table so the application can count how many items have been shipped.",
            "database": "postgresql+psycopg://appuser:root@localhost:5434/shipments_db",
            "query": "SELECT order_item_id FROM shipment_items;",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "order_item_id"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "order_item_id"
    }
}