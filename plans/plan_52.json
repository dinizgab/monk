{
    "execution_plan": [
        {
            "id": 1,
            "description": "Retrieve the distinct product_ids that have been ordered (from Order_Items).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3308/orders_db",
            "query": "SELECT DISTINCT product_id FROM Order_Items;",
            "depends_on": [],
            "join_info": null
        },
        {
            "id": 2,
            "description": "Retrieve product_ids from Products that are not present in the ordered product_ids returned by step 1 (these are products that have not been ordered).",
            "database": "mysql+pymysql://appuser:apppass@localhost:3307/products_db",
            "query": "SELECT product_id FROM Products WHERE product_id NOT IN ($step1.product_id);",
            "depends_on": [
                1
            ],
            "join_info": {
                "type": "LEFT",
                "on": {
                    "current_step_column": "product_id",
                    "dependency_step_column": "product_id"
                }
            }
        }
    ],
    "final_output_columns": [
        "product_count"
    ],
    "final_aggregation": {
        "type": "COUNT",
        "column": "product_id"
    }
}