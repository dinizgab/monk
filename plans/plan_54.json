{
    "execution_plan": [
        {
            "id": 1,
            "description": "Count customers that do not have any associated payment method.",
            "database": "postgresql+psycopg://appuser:root@localhost:5433/users_db",
            "query": "SELECT COUNT(*) AS missing_payment_methods FROM customers c WHERE NOT EXISTS (SELECT 1 FROM customer_payment_methods m WHERE m.customer_id = c.customer_id);",
            "depends_on": [],
            "join_info": null
        }
    ],
    "final_output_columns": [
        "missing_payment_methods"
    ],
    "final_aggregation": {
        "type": "NONE",
        "column": ""
    }
}